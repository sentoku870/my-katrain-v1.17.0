name: "Close stale issues and PRs"
on:
  schedule:
    - cron: "30 1 * * *"

jobs:
  stale:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      issues: write
    steps:
      - uses: actions/stale@v10
        with:
          operations-per-run: 200
          stale-issue-message: "This issue is stale because it has been open 90 days with no activity. Remove the stale label or comment or it will be closed in 30 days."
          close-issue-message: "This issue was closed because it has been stalled for 30 days with no activity."
          days-before-stale: 90
          days-before-close: 30
          stale-pr-message: "This PR is stale because it has been open 90 days with no activity. Remove the stale label or comment or it will be closed in 30 days."
          close-pr-message: "This PR was closed because it has been stalled for 30 days with no activity."
